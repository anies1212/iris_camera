/// Preferred output resolution/format preset.
///
/// Mirrors the presets available in the `camera` plugin to ease migration.
enum ResolutionPreset {
  low,
  medium,
  high,
  veryHigh,
  ultraHigh,
  max,
}
